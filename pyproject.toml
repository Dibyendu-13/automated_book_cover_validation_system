[project]
name = "bookleaf-validation"
version = "0.1.0"
description = "Automated Book Cover Validation System for BookLeaf Publishing"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
bookleaf-validate = "bookleaf_validation.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

